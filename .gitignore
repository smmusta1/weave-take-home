node_modules
.next
out
.env
.env.local
.DS_Store
*.log
tsconfig.tsbuildinfo
*.tsbuildinfo
.claude/
.vercel

# Raw fetched + LLM-scored data (large, rebuild via npm run pipeline:all)
data/cache/

# Note: data/derived/impact-report.json IS committed so the deployed
# dashboard has a baked snapshot to render.
